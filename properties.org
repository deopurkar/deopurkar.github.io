#+property: header-args :tangle properties.el
Format property blocks nicely.

We will export these properties.
#+begin_src elisp
  (defvar properties-to-pp '("when" "where"  "venue" "with" "comments" "links" "labels"))
#+end_src

#+RESULTS:
: properties-to-pp

Code to move to the next available line.
#+begin_src elisp
  (defun pp-properties ()
    (goto-char (point-min))
    (let ((case-fold-search t))
      (while (search-forward ":PROPERTIES:" nil t)
        (search-forward ":END:")
        (insert "\n")
        (insert "#+begin_properties\n")
        (mapcar
         (lambda (p)
  	 (and (org-entry-get (point) p)
  	      (insert
  	       (format
  		"| *%s* | %s |\n"
  		p
  		(org-entry-get (point) p)))))
         properties-to-pp)
        (insert "#+end_properties\n")
        )))
#+end_src

#+RESULTS:
: pp-properties

#+begin_src elisp
  (defun my/pp-properties-hook (backend)
    (pp-properties))
#+end_src

#+RESULTS:
: my/pp-properties-hook
